<!--User info画面-->
<!--部分テンプレートになるファイル-->
<!--/users/何かしらのid にアクセスされたとき、このファイルが表示されるのか？-->
<h2>User info(app/views/books/_list.html.erb)</h2>
          <!--プロフィール画像-->
          <%= image_tag book.user.get_profile_image(100,100) %>
          <table class="table table-striped">
            <tbody>
              <!--name-->
              <h3><%= user.name %></h3>
              <!--introduction-->
              <h3><%= user.introduction %></h3>
            </tbody>
          </table>
          <% if user == current_user %>
          <p><%= link_to "プロフィール編集", edit_user_path(user) %></p>
          <% end %>
          
          
          <!--投稿データの新規投稿画面-->
          <h2 class="mt-3">New book</h2>
          <!-- form_with部分 -->
          <%= form_with model: book do |f| %>
          <h4>本のタイトル</h4>
          <%= f.text_field :title %>

          <h4>感想</h4>
          <%= f.text_area :body %>

          <%= link_to 'Create Book', create_bookt_path, class: "btn btn-secondary" %>
          <% end %>
